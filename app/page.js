import React from 'react'
import { metadata } from '@/component/GenerateMetaData';

export const getMetadata = () => metadata;

const page = () => {
  return (
    <div>
      Hello
    </div>
  )
}

export default page
